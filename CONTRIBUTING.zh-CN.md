# 贡献说明

[English](CONTRIBUTING.md) · 简体中文

NiriBridge 是采用 GPL-3.0-or-later 的早期测试版。除非另有明确约定，贡献按项目相同许可证提交；保留第三方版权和许可声明。首个开发验证平台为 Ubuntu 26.04 和 Niri 26.04。

## 开始前

阅读 [需求](docs/requirements.zh-CN.md) 和 [设计](docs/design.zh-CN.md)，区分已经确认的行为、待选方案及未验证能力。不要把其他合成器上可用的后端视为已经在 Niri 上验证。

优先解决可以复现的问题，保持变更范围集中。涉及输入控制权、锁屏和手势等用户工作流时，先明确预期行为。

## 问题报告

提供系统、Niri 与程序版本，相关显示器缩放、输入设备类型、复现步骤和预期结果。只附经过清理的诊断信息，不包含密钥、按键记录、剪贴板内容、真实网络地址或设备唯一标识。

安全问题参见 [安全说明](SECURITY.zh-CN.md)。

## 代码与验证

- 按 [构建与测试](docs/testing.zh-CN.md) 运行常规检查。隔离集成测试与实际桌面测试的范围不同，分别报告结果。
- 修改状态转换、按键释放、边缘映射或网络协议时，提供针对相应行为的验证。
- 输入端到端测试需注明发送端和接收端的环境，以及实际通过和未通过的项目。
- 新依赖与复用代码应有明确来源，并检查其许可证要求。
- 示例使用通用设备名称，不提交本地环境记录、生成凭据或原始诊断数据。

## 界面与翻译

英文是界面源语言；即时翻译使用 `_()`，稍后翻译的消息用 `N_()` 标记。中文目录为 `ui/locales/zh_CN.json`，`python3 -B ui/test_i18n.py` 检查覆盖范围与格式占位符。设备名称、指纹、协议字段和路径属于数据，不翻译。

修改界面后检查英文和中文渲染，特别是长按钮与对话框。切换语言必须保留未保存的输入，且不重启后台共享。后端与窗口独立运行；只有两端确认屏幕设置保存成功后，界面才可报告成功。
