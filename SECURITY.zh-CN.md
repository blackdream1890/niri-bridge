# 安全反馈说明

[English](SECURITY.md) · 简体中文

## 当前状态

项目当前为早期测试版，没有长期支持的发行版本。已实现双方证书认证的 TLS 1.3、双向输入转发、按键状态清理和会话状态监测。隔离集成测试与两机验收分别记录；测试通过不等于独立安全审计。

`capture-test` 仅统计事件数量，支持 Escape、超时与输入焦点丢失时退出，并有独立子进程超时保护。真实桌面的测试需要事先确认时机。

## 私密反馈

安全问题通过本仓库的 GitHub 私人漏洞报告提交。目前不承诺响应时限。

不要在公开 Issue 中披露未修复漏洞的利用细节、个人日志或任何凭据。

## 已实现的边界

- 未认证对端不能注入输入，网络输入事件经过认证和加密。
- 程序不记录键盘输入内容、剪贴板内容或密钥。
- 输入权限范围尽可能小，连接失效后恢复本机控制并释放模拟按键。
- 安全相关行为以实际实现、测试和审查为依据。

连接只信任显式配对的证书；错误的服务器名称、未配对客户端和未信任服务端都有拒绝测试。协议限制帧大小、事件数值、时序及序号；原生触摸板另校验能力、轴范围和触点数量。连接超时、写入阻塞或队列过旧时结束共享。普通用户进程读取选定实体设备；锁屏和会话不可用时关闭读取句柄，释放触摸板独占、Wayland 捕获、模拟按键和触点。

`uaccess` 按当前活动用户授予设备访问，不隔离同一用户下的其他程序。管理员安装器的授权范围、权限和防火墙回退见 [安装说明](docs/setup.zh-CN.md)。不要将用户服务改为 root 运行，不要放宽私钥权限，不要把虚拟输入设备加入实体设备列表。

## 桌面管理与配置

界面仅通过同一用户的私有 Unix socket 管理服务，不开放 Web 管理端口或通用输入注入接口。配对导入使用冻结的公开证书，并重新匹配已核对的指纹；拒绝私钥文件和本机证书。

双端屏幕变更仅涉及入口显示器与范围，核对两端配置版本，不修改认证、设备授权或 Niri 内部布局。配置原子写入；未完成操作仅在文件仍匹配本次写入时回滚，保留外部编辑。断网导致确认不完整时需重连后检查两端。

安全问题请通过 [GitHub 私人漏洞报告](https://github.com/blackdream1890/niri-bridge/security/advisories/new) 提交。附上受影响版本、复现步骤与经过检查的诊断信息，不在公开 Issue 中提交凭据、配对文件或个人信息。发行包随附 SHA-256 校验和及对应源码。
