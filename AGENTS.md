# 开发约定

## 范围与需求

- 默认使用简体中文沟通。
- 开始工作前阅读 README.md、docs/requirements.md 与 docs/design.md，区分已确认需求、设计提案和未验证能力。
- 核心目标是两台 Ubuntu/Niri/Wayland 设备间双向共享键鼠，键盘随指针切换。两端触摸板、鼠标及键盘输入来源都必须纳入验证。
- 已有 KDE Connect 时由其负责剪贴板同步，不增加第二套机制，也不让键鼠共享依赖剪贴板服务。
- 接管冲突、锁屏、手势等未确定的工作流不得静默决定；先澄清，同时继续独立工作。
- 不依赖示例主机名、设备节点编号、显示器位置或缩放常量，读取实际能力与配置。
- 按未来可公开维护的项目处理代码和资料。个人环境记录放在 Git 忽略的 .local/ 中；公开示例使用通用设备名称。
- 项目采用 GPL-3.0-or-later。复用外部代码时记录来源与许可要求，保留第三方版权和许可声明。
- 公开提交使用公开署名和 GitHub 隐私邮箱。发行物必须从经过检查的源码构建；不得包含个人路径、设备标识、配置、凭据或未经清理的日志。

## 授权与安全

- 本地开发、只读探查、依赖和测试遵循用户的授权范围。
- 修改输入设备权限、凭据、认证配置、防火墙或系统服务，以及在日常桌面部署，须有该动作的明确授权。
- 提交、推送、发布和远端仓库变更须有明确授权，不从 Git 初始化或连接配置推断提交与推送授权。
- 不自动独占正在使用的键鼠或向活动桌面发送测试按键；交互测试先说明范围、紧急返回方式并确认测试时机。
- 不记录或提交按键内容、剪贴板内容、密钥、令牌、设备唯一标识或未经清理的日志。
- 保留现有用户配置和未提交改动；不绕过 SSH 主机验证或其他安全机制。

## 实现与验证

- 优先使用已验证的接口及成熟认证、加密实现；设备权限集中在尽可能小的职责范围内。
- 防止虚拟输入回流；按键与按钮状态必须在断线、退出与切换时正确释放。
- 有实际代码后增加针对状态转换、断线释放、坐标映射及协议处理的有效测试。
- 端到端通过必须以两台设备的实际行为为证据；编译成功不代表共享键鼠成功。
- 常规检查：`cargo fmt --all -- --check`、`cargo clippy --locked --all-targets -- -D warnings`、`cargo test --locked --all-targets`。
- 后台隔离的输入集成测试：`cargo test --locked --test nested_capture -- --ignored --nocapture`，需要已安装 Weston 和 Niri。测试自行创建临时显示服务，合成输入只能发送到该测试创建的 socket。
- 完整协调器回归：`cargo test --locked --test encrypted_bridge -- --ignored --nocapture --test-threads=1`；实际 uinput 触摸板探测与这些隔离测试分开执行，范围见 `docs/testing.md`。
- `capture-test` 会影响指定的实际桌面，运行前确认测试时机；默认 doctor 与配置检查不应具有输入副作用。
